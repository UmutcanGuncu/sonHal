@model List<InboxContactDto>
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                <div class="email-left-box">
                    @await Html.PartialAsync("_SideBarContactPartial")
                    <h5 class="mt-5 m-b-10">Categories</h5>
                    @await Html.PartialAsync("_SideBarContactCategoryPartial")
                </div>
                <div class="email-right-box">
                    <div role="toolbar" class="toolbar">
                        <div class="btn-group">
                            <button aria-expanded="false" data-toggle="dropdown" class="btn btn-dark dropdown-toggle" type="button">
                                Gelen Mesajlar <span class="caret m-l-5"></span>
                            </button>

                        </div>
                    </div>
                    <div class="email-list m-t-15">
                        @foreach (var item in Model)
                        {
                            <div class="message">
                                <a href="/Admin/Contact/ReceiveMessageDetails/@item.Id">
                                    <div class="col-mail col-mail-1">
                                        <div class="email-checkbox">
                                            <input type="checkbox" id="chk2">
                                            <label class="toggle" for="chk2"></label>
                                        </div><span class="star-toggle ti-star"></span>
                                    </div>
                                    <div class="col-mail col-mail-2">
                                        <div class="subject "> <span class="text-start text-danger">@item.Name</span>               @item.Subject </div>
                                        <div class="date">@(((DateTime)item.Date).ToString("dd-MMM-yyyy"))</div>
                                    </div>



                                </a>
                            </div>
                        }

                    </div>
                  

                </div>
            </div>
            </div>
        </div>
    </div>
